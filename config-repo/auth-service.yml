server:
  context-path: /uaa
  port: 5000

management:
  security:
    enabled: false
  context-path: /

eureka:
  instance:
    health-check-url-path: /uaa/health
    status-page-url-path: /uaa/info
    metadata-map:
      management.context-path: /uaa/

spring:
  datasource:
    url: jdbc:h2:mem:user
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: true
